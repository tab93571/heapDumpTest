1. It appears that we need to bring private ip from deRegister lambda to getAccessToken lambda to
get_heap_dump lambda, can you just store somewhere temporarily and get_heap_dump lambda can access that place.

2. Should we also store the variables close_unhealthy_task lambda somewhere temporarily